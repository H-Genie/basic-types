const whatisUndefined: string = "값을 할당하지 않은 변수 / 무언가가 아예 준비 안된 상태";
const wahtIsNull: string = "무언가가 있는데 사용할 준비가 덜 된 상태";

// ================================

let u: undefined = null;
let v: void = undefined;

let union: string | null = null;
union = "Mark";
